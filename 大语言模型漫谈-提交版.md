# **大语言模型漫谈（科普情景类）**
## **两句话并不矛盾**
这两句话描述的是同一个东西的两个不同层面，所以并不矛盾。机制层面：模型在物理上做的每一件事，都是“根据已有文本预测下一个 token 的概率分布”；表现层面：经过特定数据与训练方式的塑造，它在用户面前呈现的行为是“像在理解要求并回答问题”。机制从未改变，改变的是训练塑造出来的行为。下面我从机制、训练、追问三个层面展开，并在最后附上我的查证与修正记录。
## **第 1 层 · 机制：
LLM 是基于深度学习、在海量文本数据上训练的超大规模模型，基于谷歌提出的 Transformer 架构，依靠自注意力机制理解上下文关系。但在生成文本这件事上，它本质上是一台概率计算器：统计文字规律，输出“下一个 token 是谁”的概率分布。当预测精度足够高时，“像理解”会作为一种涌现现象出现——但那是后话，先看它的机制。
先讲三个关键词，都是我查证以后自己写的：
1. Token（词元）：模型不直接读汉字或单词，而是先把文本切成一块块的小单元，叫 token。中文里一个词、甚至一个字都可能是一个 token；英文里一个单词常被切成几个子词 token。整段输入在模型眼里就是“一串 token 的编号”。例如“西安电子科技大学”，可能被切分为“西安”“电子”“科技”“大学”四个 token。
2. 概率分布：输入一串 token 后，模型并不直接“说出”下一个词，而是给词表里的每一个 token 算出一个“它作为下一个 token 的概率”。比如输入“今天天气真”，模型可能认为“好”的概率 0.4、“热”0.3、“不错”0.2……所有概率加起来等于 1。
3. 自回归生成：模型从这一串概率里挑一个 token（按概率随机选，这就是“采样”），把它拼回输入末尾，再预测下一个，循环往复，直到输出一个“结束”标记。我们看到的整段回答，就是这么一点点写出来的。
所以，从机制上说，“LLM 是反复预测下一个 token 的机器”这句话很准确。我实际试过一次：让它续写“今天太阳好”，它接的是“今天太阳好大，出门记得做好防晒”——它不是在复读，而是顺着统计规律补出了“太阳大→要防晒”这类常见关联。这个例子正好引出下一层：为什么它接得“像懂常识”。
## **第 2 层 · 表现：“像理解”是训练一步步“教”出来的**
如果机制只是接话茬，为什么它用起来像理解？我查证后的理解是：这个“像”字，是训练过程一步步“教”出来的。
第一步，预训练：把知识压进参数。
模型先在海量互联网文本（网页、书籍、论文……）上反复练习“预测下一个词”。这个任务听起来很笨，但要做到预测得准，它被迫学会很多东西：语法规则、常识、事实、推理模式。这些知识不是存进一个“数据库”里，而是以数字的形式编码在模型几十亿、上千亿个参数里。
OpenAI 首席科学家 Ilya Sutskever 曾在访谈中表达过类似的观点：准确预测下一个 token，本质上是在压缩世界的信息——压缩得越好，理解越深。模型必须懂得语法规则、语义关联（例如“苹果”在水果语境和科技语境中含义不同）、逻辑关系、世界知识与上下文推理。当然，这里的“理解”并非人类意义上的意识体验，而是模型在参数中编码的、对语言结构和世界规律的表示。为什么它“像理解”呢？因为语言本身也是一种模式：LLM 通过预测下一个词，学习了词语间的共现规律、句法结构的嵌套关系（主谓宾、定状补）、语义场的聚类（教室、老师、学生）和逻辑推理的链条（假言、选言）。模型确实捕捉到了语言背后的结构和规律；但它至少目前并没有可靠的世界模型，也很难提出训练数据之外的全新观念。因此：预测下一个词是手段，“像理解”是结果。
第二步，指令微调（SFT）：学会“问-答”的格式。
预训练出来的模型只会“续写”，你问它问题，它可能继续写一篇小说。于是人们用大量“用户提问-标准回答”的对子继续训练它。从此，当输入以问号结束时，模型学到的规律是：“最合理的下一个 token 序列，是一个答案。”
第三步，人类反馈对齐（RLHF）：学会“像一个有用的助手”。
人类给模型的多个回答打分排序，模型再学习“哪种回答更好”——更完整、更礼貌、更像一个认真负责的助手，而不是冷冰冰地接话。
到这里，“不矛盾”就成立了：模型的目标从头到尾都是“预测下一个词”，但训练数据里“问号后面跟着答案”是压倒性的统计规律。它“接话茬”接得足够好，好到“像理解”——其实是统计规律在扮演理解。
## **第 3 层 · 追问：这算“真理解”吗？**
如果停留在这里，回答只是复述资料。我继续追问了两个问题。
追问一：它是“记住”了还是“理解”了？
训练时见过的问题，它靠“回忆”；没见过的问题，它靠“模式迁移”——把学过的规律套到新问题上。它能做训练数据里没有的数学变体题，说明它不是死记硬背；但它也会在没见过的事实上编造答案，说明它也没有真正的“验证”。我的结论是：它的能力介于“记忆”和“理解”之间，更像一种高水平的模式匹配。
追问二：幻觉为什么必然存在？
因为它的生成目标永远是“输出最可能的续写”，而不是“核对事实”。它没有“我不知道”这个选项——当所有候选概率都不高时，它仍然会挑一个相对最高的说出来。所以幻觉不是 bug，而是“预测下一个词”这个机制的自然副产品。
我的修正记录：
我最初以为，LLM 就像一个巨大的搜索引擎，把知识存在数据库里，回答问题时“查”出来。
查证后我发现不是：它的知识编码在参数里，每次回答是从参数里“重建”一句话，而不是从数据库里“检索”一条记录。这修正了我对“模型知道什么”的理解——它更像“会背很多书的记忆体”，而不是“图书馆”。
一个更深的视角（我读题时联想到的）：
这道题让我想起文件前言里的例子——“为什么用 LLM 写代码必须明确约束”。自然语言需求是“欠定”的：同一句话可以对应多种不同但都看似合理的程序。模型在信息不足时，只能用训练形成的先验去“补全”缺失的部分。这也是为什么“预测下一个词”既是它的能力来源，也是它的局限来源：它补全的是“最可能的”，不一定是“正确的”。
## **结尾：**
LLM 是一台读遍人类文字、学会了“什么词最该接在什么词后面”的概率机；训练数据里的问答模式和人类偏好让它表现得像在理解；它的知识是参数的压缩而不是数据库的检索；因此它擅长模仿规律、组织语言，但不擅长保证事实与计算——这也是为什么和它协作时，我们需要把需求说得足够明确。
这套理解对我最大的改变是：我不再把 AI 当作“全知者”，而是当作“一个读过很多书、很会接话、偶尔会自信地胡说八道的搭档”。知道它为什么会胡说，我反而更知道怎么用好它。
## **第 4 层 · 姊妹问题选答（我选 π 问题）**
三个姊妹问题里，我选“用 π 训练一台吐数字的机器，能不能帮我们解决计算 π 的问题”来深入，因为它最能检验我对“预测 vs 计算”的理解。我查到的一个真实案例：知乎上有人讨论过“如果把 π 的前一万亿位喂给大模型会怎样”，已有开发者实际做过实验——用长度为 64 的数字序列训练模型后，最终 loss 稳定在 2.3026，预测准确率在 10% 附近波动，和随机瞎猜一模一样。这个数字特别说明问题：loss = 2.3026 恰好等于 ln(10)，意味着模型学到的就是“十个数字完全等概率”，一点规律都没学到——π 并无统计规律可言。
这背后的原因是：预测 ≠ 计算。LLM 的能力建立在数据的统计规律之上，但 π 的小数无限不循环，目前普遍认为其数字近似均匀随机分布（即著名的“正态性猜想”），统计上不存在可供模型学习的规律，训练集之外的下一位数字只能靠猜。计算 π 需要的是算法（例如 Chudnovsky 公式这样的级数方法），而不是统计——即使模型在训练数据里“见过”这个公式，它也只是背出了公式，无法从 π 的数字序列本身反推出计算规则。
所以，用 π 训练一台“数字的机器”，不能解决计算 π 的问题：它能“背”，不能“算”。这恰好印证了 next-token 预测的能力边界——它擅长捕捉有规律可循的模式，面对本质上不可预测的问题无能为力。
## **查证与修正记录**

| 我查了什么 | 我原本以为 | 查证后修正为 | 来源 |
| --- | --- | --- | --- |
| token / 自回归生成 | 模型直接生成整段话 | 模型逐 token 预测并采样拼接 | Karpathy《Deep Dive into LLMs like ChatGPT》https://www.bilibili.com/video/BV1dDQdBjESP；3Blue1Brown《GPT是什么？》https://www.bilibili.com/video/av1353986541/ |
| 模型的知识存在哪 | 像搜索引擎存在数据库 | 编码在参数里，回答是“重建” | Karpathy 同视频（模型权重是训练数据的压缩，知识编码在参数中）https://www.bilibili.com/video/BV1dDQdBjESP |
| 预训练 / SFT / RLHF 的作用 | 训练只为了让模型变聪明 | 不同阶段塑造不同行为（续写→问答→对齐） | Karpathy 同视频（预训练→SFT→RLHF 三阶段讲解）https://www.bilibili.com/video/BV1dDQdBjESP |
| Ilya Sutskever“预测即压缩”的说法 | 直接引用原话 | 需要核实出处，宜表述为“曾表达过类似观点” | Ilya Sutskever × Jensen Huang 炉边对谈（NVIDIA GTC 2023）：https://www.nvidia.com/en-us/on-demand/session/gtcspring23-s52092/（“好的数据压缩会带来无监督学习”“压缩会揭示数据中的秘密”） |
| π 数字的统计性质 | 也许能外推 | 近似均匀随机分布，无可预测规律；loss≈ln(10) 说明与瞎猜无异 | ① 论文《Can a Language Model Generate π Digits?》https://d2j16w31g89z0j.cloudfront.net/site/2024f/abs/0632-CKRDE.pdf ② π 正态性猜想（尚未证明）：Scientific American https://www.scientificamerican.com/article/does-pi-contain-all-of-shakespeare/ |
