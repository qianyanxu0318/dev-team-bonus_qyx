# with 漫谈：为什么 Python 要有 with 语句？



---

## 一、开头：我目前对 with 的理解

`with` 是 Python 里一种和"资源"有关的语法。我目前的理解可以浓缩成一句话：**它用来保证"打开的东西一定会被关闭"。**

最典型的例子是文件读写：文件打开之后必须关闭，而 `with` 就是让"关闭"这件事**不需要我记着去做**的语法。下面我用自己遇到的问题来说明它为什么存在。

## 二、为什么需要 with：资源是会"漏"的

我刚开始学文件操作时写过这样的代码：

```python
f = open('笔记.txt', 'w')
f.write('hello')
```

看起来没什么问题，但有个隐患：**我忘了 `f.close()`**。

打开一个文件，向操作系统申请的是一个"文件句柄"——这是一种稀缺资源：数量有限、占用期间别的程序无法使用这个文件（比如删不掉、改不了），开得多了还会把句柄耗尽。所以"打开"和"关闭"必须成对出现，就像开门必须关门。

但问题在于：**关门很容易被忘掉，尤其是代码中途出错的时候**。如果 `f.write('hello')` 这一行抛了异常，程序直接跳走，后面的 `f.close()` 就永远执行不到了——门就那样开着。

Python 其实早就给了我们一个"保证关门"的办法：`try/finally`。

```python
f = open('笔记.txt', 'w')
try:
    f.write('hello')
finally:
    f.close()      # 无论上面是否出错，这一行一定执行
```

这个写法是对的，但我发现它有两个缺点：**样板代码多**（每用一次资源都要包一层 `try` 和 `finally`），**关闭写在离打开很远的地方**（代码长了容易看漏）。PEP 343 提出 `with` 语句（由 Guido van Rossum 与 Nick Coghlan 提出，Python 2.5 引入），要解决的正是这两个问题。

## 三、with 的用法：把"保证关闭"变成语法

同样一件事，用 `with` 写是这样：

```python
with open('笔记.txt', 'w') as f:
    f.write('hello')
```

我来解释一下这行代码：

- `open('笔记.txt', 'w')` 照常打开文件；
- `as f` 把打开的文件对象赋值给变量 `f`，后面用它读写；
- 最关键的是：**只要缩进里的代码执行完——不管是正常结束还是中途出错——文件都会被自动关闭**。

也就是说，`with` 把我上面手写的 `try/finally` 那套"保证关闭"的逻辑，**打包成了语言自带的一个语法结构**。我不用再记着写 `f.close()`，也不用担心出错时漏掉关闭。

我还试过同时打开多个文件，`with` 同样适用：

```python
with open('a.txt') as fa, open('b.txt') as fb:
    content_a = fa.read()
    content_b = fb.read()
# 两个文件都会自动关闭
```

## 四、我还在学习中的部分（诚实说明）

我在查资料时了解到，`with` 背后其实还有一套更深的机制：对象只要实现 `__enter__` 和 `__exit__` 两个方法就能被 `with` 使用，这套机制叫"上下文管理器协议"；标准库里还有 `contextlib`、`pathlib` 相关的进阶用法。这些我目前只看了资料、还没有完全掌握，所以这篇答卷没有展开写。

这也是我接下来想继续弄懂的方向：**先把自己真正理解的部分写清楚，再一步步把"协议"这块啃下来。**

用一句话总结我现在对 `with` 的理解：

> `with` 是一句"保证善后"的承诺——你只管在缩进里做事，退出的事交给它。它把"打开→使用→关闭"变成了一个完整、不容易出错的语法结构，让我这种初学者不用再担心"忘记关闭"。

## 五、查证与修正记录

| 我查了什么 | 我原本以为 | 查证后修正为 | 来源 |
|---|---|---|---|
| 为什么文件用完后要 close | 关不关无所谓 | 文件句柄是稀缺资源，不关闭会占用、泄露 | https://docs.python.org/zh-cn/3/library/io.html|
| with 是什么 | 打开文件专用的语法 | 是"保证关闭"的通用语法，文件只是最常见用法 | PEP 343 https://peps.python.org/pep-0343/ |
| with 和 try/finally 的关系 | 完全不同的两种写法 | with 的底层就是包装好的 try/finally | https://docs.python.org/zh-cn/3/tutorial/errors.html|


